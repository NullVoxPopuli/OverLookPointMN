<h1>New poll</h1>
You must first creat the poll, then go to edit the poll to add the different options.
<% form_for([:admin, @poll]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :expiration %><br />
    <%= f.date_select :expiration %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_polls_path %>